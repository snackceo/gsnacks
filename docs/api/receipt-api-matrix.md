# Receipt API Matrix

This document has been consolidated.

Use the canonical Receipt API matrix in operations docs:

- [docs/ops/receipt-api-matrix.md](../ops/receipt-api-matrix.md)

## Why

Historically, this repository had two receipt matrix files that drifted over time.
To prevent future discrepancies, the ops matrix is now the single source of truth.

## Maintenance rule

When endpoints are added/updated/deprecated, update only:

- `docs/ops/receipt-api-matrix.md`
